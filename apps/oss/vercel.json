{
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@latest --activate && pnpm install --no-frozen-lockfile && pnpm build:packages",
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "framework": "nextjs"
}
